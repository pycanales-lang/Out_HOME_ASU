<!DOCTYPE html>
<html lang="es">
<head>
<script async src="https://www.googletagmanager.com/gtag/js?id=G-8QS0FFX4MB"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-8QS0FFX4MB');
</script>

<meta charset="UTF-8">
<title>CALL ENTRY HOME | Competencia Comercial 2026</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #1e3a8a, #2563eb);
        margin: 0;
        padding: 20px;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        -webkit-user-select: none;
        user-select: none;
    }

    .container {
        max-width: 500px;
        width: 100%;
        background: #ffffff;
        padding: 30px;
        border-radius: 16px;
        box-shadow: 0 15px 35px rgba(0,0,0,0.3);
        position: relative;
        overflow: hidden;
    }

    /* --- ESTILO DE TÍTULOS MEJORADO --- */
    .header-main {
        text-align: center;
        margin-bottom: 25px;
    }

    .tag {
        display: inline-block;
        background: #fbbf24;
        color: #1e3a8a;
        padding: 4px 15px;
        border-radius: 20px;
        font-weight: 800;
        font-size: 11px;
        margin-bottom: 15px;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    h1 {
        color: #1e3a8a;
        margin: 0;
        font-size: 36px;
        font-weight: 900;
        line-height: 0.9;
        letter-spacing: -1px;
    }

    .sub-title {
        color: #2563eb;
        font-size: 20px;
        font-weight: 300;
        letter-spacing: 5px;
        margin-top: 5px;
        text-transform: uppercase;
    }

    /* --- BARRA DE TIEMPO --- */
    .timer-section {
        background: #f8fafc;
        border-radius: 12px;
        padding: 15px;
        margin-bottom: 20px;
        text-align: center;
        border: 1px solid #e2e8f0;
    }

    .timer-title {
        font-size: 11px;
        font-weight: bold;
        color: #64748b;
        margin-bottom: 8px;
        text-transform: uppercase;
    }

    .progress-container {
        background: #e2e8f0;
        border-radius: 10px;
        height: 8px;
        overflow: hidden;
        margin: 10px 0;
    }

    .progress-bar {
        height: 100%;
        background: linear-gradient(90deg, #2563eb, #10b981);
        width: 0%;
        transition: width 1s ease;
    }

    .countdown {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-top: 5px;
    }

    .count-item { font-size: 18px; font-weight: bold; color: #1e3a8a; line-height: 1; }
    .count-label { font-size: 9px; color: #94a3b8; display: block; text-transform: uppercase; margin-top: 4px; }

    /* Marca de agua */
    .watermark {
        position: absolute;
        top: 50%; left: 50%;
        transform: translate(-50%, -50%) rotate(-30deg);
        font-size: 45px;
        font-weight: 900;
        color: rgba(0,0,0,0.03);
        white-space: nowrap;
        pointer-events: none;
        z-index: 0;
    }

    /* --- TABLA --- */
    table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
        font-size: 13px;
        position: relative;
        z-index: 1;
    }

    th {
        background: #f1f5f9;
        color: #475569;
        padding: 10px;
        text-align: left;
        border-bottom: 2px solid #e2e8f0;
        text-transform: uppercase;
        font-size: 11px;
    }

    td { padding: 12px 10px; border-bottom: 1px solid #f1f5f9; }

    .count-badge { font-weight: bold; color: #2563eb; font-size: 15px; text-align: center; }
    .qty-badge { font-weight: bold; color: #059669; text-align: center; background: #ecfdf5; border-radius: 6px; padding: 4px; }
    .prize-title { font-weight: bold; color: #1e293b; }
    .prize-sub { font-size: 11px; color: #64748b; }

    .criterios-box {
        background: #fffbeb;
        padding: 15px;
        border-radius: 8px;
        border-left: 4px solid #fbbf24;
        margin-top: 10px;
    }

    .criterios-box h2 { font-size: 14px; color: #92400e; margin-top: 0; margin-bottom: 8px; }
    ul, ol { padding-left: 18px; font-size: 12px; line-height: 1.4; color: #451a03; margin: 5px 0; }

    .footer {
        margin-top: 25px;
        font-size: 10px;
        color: #94a3b8;
        text-align: center;
        text-transform: uppercase;
        letter-spacing: 1px;
    }
</style>
</head>
<body>
  
<div class="container">
    <div class="watermark">USO INTERNO • CANALES</div>

    <div class="header-main">
        <span class="tag">Competencia Asesores 2026</span>
        <h1>ENTRY HOME</h1>
        <div class="sub-title">Call Digital</div>
    </div>

    <div class="timer-section">
        <div id="timer-status" class="timer-title">Cargando competencia...</div>
        <div class="progress-container">
            <div id="p-bar" class="progress-bar"></div>
        </div>
        <div class="countdown">
            <div class="count-item"><span id="days">00</span><span class="count-label">Días</span></div>
            <div class="count-item"><span id="hours">00</span><span class="count-label">Horas</span></div>
            <div class="count-item"><span id="mins">00</span><span class="count-label">Min</span></div>
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th style="text-align: center;">Ganadores</th>
                <th>Premios</th>
                <th style="text-align: center;">QTY Inst.</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="count-badge">2</td>
                <td><div class="prize-title">Speaker GO 4</div><div class="prize-sub">JBL</div></td>
                <td><div class="qty-badge">50</div></td>
            </tr>
            <tr>
                <td class="count-badge">2</td>
                <td><div class="prize-title">Vale Super Gs. 200.000</div><div class="prize-sub">Supermercados</div></td>
                <td><div class="qty-badge">49</div></td>
            </tr>
            <tr>
                <td class="count-badge">2</td>
                <td><div class="prize-title">Hoppy Azul</div><div class="prize-sub">BUBBA</div></td>
                <td><div class="qty-badge">48</div></td>
            </tr>
            <tr>
                <td class="count-badge">2</td>
                <td><div class="prize-title">Mochila Tigo</div><div class="prize-sub">Tigo</div></td>
                <td><div class="qty-badge">47</div></td>
            </tr>
            <tr>
                <td class="count-badge">2</td>
                <td><div class="prize-title">Waflera</div><div class="prize-sub">Super Practico</div></td>
                <td><div class="qty-badge">46</div></td>
            </tr>
            <tr>
                <td class="count-badge">2</td>
                <td><div class="prize-title">Vale Gs. 100.000</div><div class="prize-sub">Combustible</div></td>
                <td><div class="qty-badge">45</div></td>
            </tr>
        </tbody>
    </table>

    <div class="criterios-box">
        <h2>📌 Criterios HOME</h2>
        <ul>
            <li>Generadas: 01 al 14 de Enero 2026.</li>
            <li>Instaladas: hasta el 18 de Enero 2026.</li>
        </ul>

        <hr style="border: 0; border-top: 1px solid #fde68a; margin: 10px 0;">
        <h2 style="font-size: 13px;">⚖️ Empate:</h2>
        <ol>
            <li>Mayor cantidad de generadas.</li>
            <li>Mayor revenue / Mayor Pospago.</li>
        </ol>
    </div>

    <div class="footer">
        © 2026 – CANALES | Información Exclusiva Interna
    </div>
</div>

<script>
    const startDate = new Date('2026-01-01T00:00:00').getTime();
    const endDate = new Date('2026-01-14T23:59:59').getTime();

    function updateTimer() {
        const now = new Date().getTime();
        const totalDuration = endDate - startDate;
        const pBar = document.getElementById('p-bar');
        const statusText = document.getElementById('timer-status');
        
        if (now < startDate) {
            statusText.innerText = "LA COMPETENCIA INICIA EN:";
            showTime(startDate - now);
            pBar.style.width = "0%";
        } else if (now > endDate) {
            statusText.innerText = "COMPETENCIA FINALIZADA";
            pBar.style.width = "100%";
            pBar.style.background = "#ef4444";
            document.getElementById('days').innerText = "00";
            document.getElementById('hours').innerText = "00";
            document.getElementById('mins').innerText = "00";
        } else {
            statusText.innerText = "TIEMPO PARA EL CIERRE:";
            const remaining = endDate - now;
            const progress = ((now - startDate) / totalDuration) * 100;
            pBar.style.width = progress + "%";
            
            if (remaining < (3 * 24 * 60 * 60 * 1000)) {
                pBar.style.background = "#ef4444";
                statusText.style.color = "#ef4444";
            }
            showTime(remaining);
        }
    }

    function showTime(ms) {
        document.getElementById('days').innerText = Math.floor(ms / (1000 * 60 * 60 * 24)).toString().padStart(2, '0');
        document.getElementById('hours').innerText = Math.floor((ms % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)).toString().padStart(2, '0');
        document.getElementById('mins').innerText = Math.floor((ms % (1000 * 60 * 60)) / (1000 * 60)).toString().padStart(2, '0');
    }

    setInterval(updateTimer, 1000);
    updateTimer();

    document.addEventListener('contextmenu', e => e.preventDefault());
</script>

</body>
</html>
